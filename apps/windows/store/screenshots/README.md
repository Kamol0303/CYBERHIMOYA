# Screenshot capture notes

1. Run API + WPF: `CGA_API_BASE=http://127.0.0.1:8000 dotnet run`
2. Capture MainWindow at 1280×800 and 1920×1080
3. Show clean + malicious sample URL results
4. Drop PNGs here before Partner Center upload (git-lfs optional)
