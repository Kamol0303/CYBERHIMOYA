# Windows WPF host

```bash
cd apps/windows/src/CyberGuardian.Windows
dotnet build
dotnet run
```

Requires .NET 8 SDK (Windows). Set `CGA_API_BASE=http://127.0.0.1:8000`.

UI: `MainWindow.xaml` — brand-first scan surface. Defensive only.
